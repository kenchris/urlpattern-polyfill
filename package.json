{
  "name": "urlpattern-polyfill",
  "version": "1.0.0-rc5",
  "description": "Polyfill for the proposed URLPattern API",
  "repository": {
    "type": "git",
    "url": "https://github.com/kenchris/urlpattern-polyfill"
  },
  "exports": {
    "import": "./dist/index.mjs",
    "require": "./dist/index.js",
    "types": "./dist/index.d.ts"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "ava": "3",
    "esm": "latest",
    "rimraf": "latest",
    "tsup": "latest"
  },
  "scripts": {
    "build:esm": "tsup src/index.ts --format esm",
    "build:cjs": "tsup src/index.ts --format cjs",
    "build:types": "cp src/index.d.ts dist/index.d.ts",
    "build": "rimraf dist/ && npm run build:esm && npm run build:types && npm run build:cjs",
    "test": "npm run build && ava --fail-fast -s",
    "quicktest": "ava --fail-fast -s",
    "publish-dev": "npm test && VERSION=${npm_package_version%-*}-dev.`git rev-parse --short HEAD` && npm version --no-git-tag-version $VERSION && npm publish --tag dev"
  },
  "ava": {
    "require": [
      "esm"
    ]
  },
  "author": "",
  "license": "MIT",
  "dependencies": {}
}
